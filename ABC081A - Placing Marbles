using System;
class program{
	static void Main(){
		string a = Console.ReadLine();
		int ans = 0;
		for(int i=0;i<a.Length;i++){
			ans+=int.Parse(a.Substring(i,1));
		}
		Console.WriteLine(ans);
	}
}
